{
  "source": {
    "type": "github",
    "repository": "IBM/mcp-context-forge",
    "pr_number": 3269,
    "url": "https://github.com/IBM/mcp-context-forge/pull/3269"
  },
  "metadata": {
    "title": "[FIX][API] Token name uniqueness conflict returns generic 409 and admin UI freezes on backend errors",
    "state": "closed",
    "created_at": "2026-02-26T05:34:46+00:00",
    "updated_at": "2026-02-28T17:21:24+00:00",
    "closed_at": "2026-02-28T17:21:23+00:00",
    "merged_at": "2026-02-28T17:21:23+00:00",
    "author": "shoummu1",
    "labels": [
      "bug",
      "SHOULD",
      "api"
    ],
    "milestone": "Release 1.0.0-RC2",
    "assignees": [
      "crivetimihai"
    ]
  },
  "description": "# 🐛 Bug-fix PR\r\n\r\n## 📌 Summary\r\n\r\nFixes a deterministic silent `409` when creating a token whose name already exists for the same user within the same team scope, and adds graceful error handling to the Admin UI team selector and token table for transient backend failures (DB pool exhaustion / idle transaction timeouts).\r\n\r\nThe DB constraint enforces name uniqueness per `(user_email, name, team_id)` — scoped per team — so the same token name is allowed across different teams but blocked within the same team scope. The service pre-check, router error handlers, and error formatter are all aligned to this model.\r\n\r\n## 🔗 Related Issue\r\n\r\nCloses #3229.\r\n\r\n## 🐞 Root Cause\r\n\r\n1. **Service layer** (`mcpgateway/services/token_catalog_service.py`) — `create_token()` duplicate pre-check did not correctly mirror the DB constraint, allowing duplicates to bypass the service check, hit the DB constraint, and bubble up as the opaque generic message `Unable to complete the operation. Please try again.`\r\n\r\n2. **Router** (`mcpgateway/routers/tokens.py`) — Neither `POST /tokens` nor `POST /tokens/teams/{team_id}` caught `IntegrityError`. On a race-path duplicate the exception fell through to the global handler, producing a generic opaque message with no actionable detail.\r\n\r\n3. **Error formatter** (`mcpgateway/utils/error_formatter.py`) — The token-specific message was nested inside an outer `if \"UNIQUE constraint failed\"` block that only covers SQLite errors, meaning PostgreSQL deployments (which report the constraint name, not the column path) were not matched. The Alembic migration constraint alias `uq_email_api_tokens_user_email_name` was also not handled.\r\n\r\n4. **Admin UI** (`mcpgateway/static/admin.js`, `mcpgateway/templates/admin.html`) — Team selector used `htmx.ajax()` which silently ignores HTTP 5xx responses, leaving the dropdown visually empty with no error or retry affordance. The token table had no HTMX error listener at all. Token creation errors were only surfaced via a toast notification that could be missed if the user had scrolled.\r\n\r\n5. **PgBouncer / connection pool** (`docker-compose.yml`) — `CLIENT_IDLE_TIMEOUT` was too close to `DB_POOL_RECYCLE`, creating a race where SQLAlchemy could hand a connection back from the pool at the exact moment PgBouncer was closing it — causing `idle transaction timeout` errors and 500s under admin traffic bursts.\r\n\r\n## 💡 Fix Description\r\n\r\n**Service layer** — Pre-check query now mirrors the DB constraint exactly: `(user_email, name, team_id)`. The same token name is allowed across different teams but blocked within the same team scope (including the global no-team bucket).\r\n\r\n**DB constraint + migration** — `EmailApiToken.__table_args__` uses `UniqueConstraint(\"user_email\", \"name\", \"team_id\", name=\"uq_email_api_tokens_user_name_team\")`. Alembic migration `d9e0f1a2b3c4` handles the transition using `batch_alter_table` (SQLite + PostgreSQL safe) with idempotent upgrade/downgrade.\r\n\r\n**Router** — Added `except IntegrityError` handlers to both `create_token` and `create_team_token`. Both catch all four constraint name variants:\r\n\r\n- `uq_email_api_tokens_user_name_team` (new ORM / `db.py`)\r\n- `uq_email_api_tokens_user_name` (legacy ORM)\r\n- `uq_email_api_tokens_user_email_name` (Alembic migration)\r\n- `email_api_tokens.user_email` + `email_api_tokens.name` column path (SQLite)\r\n\r\nReturns HTTP 409 with a specific, user-actionable message instead of falling through to the global generic handler.\r\n\r\n**Error formatter** — Token constraint check moved ahead of the generic `UNIQUE constraint failed` block so it always takes priority. Single clean `if` covering all four constraint name variants.\r\n\r\n**Admin UI**\r\n- Migrated `loadTeams()` in `admin.html` and `performTeamSelectorSearch()` in `admin.js` from `htmx.ajax()` to `fetch()` — both now detect HTTP 5xx and render an inline error with a Retry button. On failure, `dataset.loaded` is cleared (`delete container.dataset.loaded`) so reopening the dropdown automatically retries.\r\n- Added `htmx:responseError` and `htmx:sendError` listeners on the tokens panel with an inline error + Retry in the token table.\r\n- Added `<div id=\"token-creation-messages\">` next to the create-token form that shows the API error inline on failure (auto-clears after 15 s) and is cleared on success.\r\n\r\n**PgBouncer / pool** — Raised `CLIENT_IDLE_TIMEOUT` to 120 s in `docker-compose.yml`, making it 2× `DB_POOL_RECYCLE=60 s` so connections are always proactively recycled before PgBouncer closes them. Corrected misleading inline comments in `docker-compose-performance.yml`.\r\n\r\n## 🧪 Verification\r\n\r\n| Test | Command / Action | Result |\r\n|---|---|---|\r\n| Lint suite | `make lint` | ✅ |\r\n| Unit tests | `make test` | ✅ 117 token / error-formatter tests pass |\r\n| Coverage ≥ 90 % | `make coverage` | ✅ |\r\n| Cross-scope duplicate → allowed | Create token N (no team), then N for a specific team | ✅ |\r\n| Same-scope duplicate → specific 409 | Create identical name + team twice | ✅ |\r\n| Race-path duplicate → specific 409 | `IntegrityError` injected via mock at router | ✅ |\r\n| Team selector error + retry on 5xx | Simulate DB outage, open team dropdown | ✅ |\r\n| Token table error + retry on 5xx | Simulate DB outage, navigate to Tokens panel | ✅ |\r\n| Token form inline error on failure | Submit form with duplicate name | ✅ |\r\n\r\n## 📐 MCP Compliance (if relevant)\r\n\r\n- [x] Matches current MCP spec  \r\n- [x] No breaking change to MCP clients\r\n\r\n## ✅ Checklist\r\n\r\n- [x] Code formatted (`make black isort pre-commit`)\r\n- [x] No secrets/credentials committed",
  "comments": [],
  "reviews": [
    {
      "id": "3865152555",
      "author": "madhav165",
      "state": "CHANGES_REQUESTED",
      "submitted_at": "2026-02-27T06:20:15+00:00",
      "body": "The patch introduces a logic bug in the team selector loading state: a failed fetch marks dataset.loaded with a truthy string, preventing automatic reload on reopen. This is a user-facing behavior regression in the admin UI.\n\nReview comment:\n- [P2] Clear failed load flag instead of storing \"false\" - admin.html: 876-876\n- When team loading fails, container.dataset.loaded is set to the string 'false', but loadTeams() checks if (container && !container.dataset.loaded). Because 'false' is truthy, reopening the dropdown will not trigger another load and leaves the selector stuck unless the user clicks the inline Retry button. This regresses the normal open-to-retry behavior after transient backend errors.\n"
    },
    {
      "id": "3870544891",
      "author": "crivetimihai",
      "state": "APPROVED",
      "submitted_at": "2026-02-28T16:04:03+00:00",
      "body": "Rebased onto latest main (clean, no conflicts). Reviewed all changes across 12 files.\n\n**Review fixes applied (2 commits):**\n\n1. **fix(tests): add missing primary constraint variant** — The parametrized IntegrityError tests covered 3 constraint name variants but missed `uq_email_api_tokens_user_name_team` (the actual current constraint from db.py). Added to both `create_token` and `create_team_token` test parametrizations.\n\n2. **fix: add partial unique index for global-scope token name uniqueness** — SQL `NULL != NULL` semantics mean `UniqueConstraint(\"user_email\", \"name\", \"team_id\")` cannot prevent duplicate global-scope tokens where `team_id IS NULL`. Added partial unique index `uq_email_api_tokens_user_name_global` on `(user_email, name) WHERE team_id IS NULL` — matching the pattern already used by resources (db.py:3306) and prompts (db.py:3724). Updated the Alembic migration with idempotent index creation/drop. Added regression tests for `db.rollback()` invocation and the global-scope partial index constraint name.\n\n**Verification:**\n- 215 unit tests pass\n- 99% coverage on affected modules (100% on new code, 4 misses are pre-existing lines)\n- Single Alembic head confirmed\n- PgBouncer timing relationship correct: `IDLE_TRANSACTION_TIMEOUT (30s) < DB_POOL_RECYCLE (60s) < CLIENT_IDLE_TIMEOUT (120s)`\n- XSS: `escapeHtml()` properly sanitizes inline error messages\n- No secrets, no schema leakage in user-facing errors"
    },
    {
      "id": "3870612357",
      "author": "crivetimihai",
      "state": "APPROVED",
      "submitted_at": "2026-02-28T17:20:33+00:00",
      "body": ""
    }
  ],
  "commits": [
    {
      "sha": "84fa49997647f4eb0e42660605b3232d7dc441f3",
      "message": "fix: resolve token name conflict semantics and admin UI freeze on backend errors\n\nSigned-off-by: Shoumi <shoumimukherjee@gmail.com>",
      "author": "Shoumi",
      "date": "2026-02-26T05:16:52+00:00"
    },
    {
      "sha": "c01a1f98b82f3b38a8dcfc601b82460f51ec20c7",
      "message": "removed duplicate const container declaration\n\nSigned-off-by: Shoumi <shoumimukherjee@gmail.com>",
      "author": "Shoumi",
      "date": "2026-02-26T05:52:07+00:00"
    },
    {
      "sha": "dadbcfaa67048b6feff885eb1e4fbcc93cac7b06",
      "message": "fix pylint & coverage issue\n\nSigned-off-by: Shoumi <shoumimukherjee@gmail.com>",
      "author": "Shoumi",
      "date": "2026-02-26T06:10:34+00:00"
    },
    {
      "sha": "a3986743d0f12122980965b0f368886b4be05bdb",
      "message": "fix: restore per-team token name uniqueness and fix admin UI reload regression\n\nSigned-off-by: Shoumi <shoumimukherjee@gmail.com>",
      "author": "Shoumi",
      "date": "2026-02-27T06:50:25+00:00"
    },
    {
      "sha": "961af713afb04c20a05ef0cf0565090f6a9a6374",
      "message": "fix tests and lint\n\nSigned-off-by: Shoumi <shoumimukherjee@gmail.com>",
      "author": "Shoumi",
      "date": "2026-02-27T07:03:41+00:00"
    },
    {
      "sha": "ca0537f5d6e2db096f93f311f0272fe63276c1ad",
      "message": "fix(tests): add missing primary constraint variant to IntegrityError tests\n\nThe parametrized IntegrityError tests for create_token and\ncreate_team_token covered 3 constraint name variants but missed\nthe primary one (uq_email_api_tokens_user_name_team) from db.py.\n\nSigned-off-by: Mihai Criveti <crivetimihai@gmail.com>",
      "author": "Mihai Criveti",
      "date": "2026-02-28T13:43:02+00:00"
    },
    {
      "sha": "4392abc63abc7260a903090ee559f11257edc1d5",
      "message": "fix: add partial unique index for global-scope token name uniqueness\n\nSQL NULL != NULL semantics mean the composite UniqueConstraint on\n(user_email, name, team_id) cannot prevent duplicate global-scope\ntokens (team_id IS NULL).  Add a partial unique index\nuq_email_api_tokens_user_name_global matching the pattern already\nused by resources and prompts for nullable foreign keys.\n\nAlso adds regression tests for db.rollback() invocation in\nIntegrityError handlers and for the global-scope partial index\nconstraint name.\n\nSigned-off-by: Mihai Criveti <crivetimihai@gmail.com>",
      "author": "Mihai Criveti",
      "date": "2026-02-28T16:00:01+00:00"
    }
  ],
  "files_changed": 12,
  "additions": 459,
  "deletions": 24,
  "merged": true,
  "mergeable": null,
  "ingested_at": "2026-05-17T15:11:34.455055",
  "_storage_metadata": {
    "stored_at": "2026-05-17T15:11:34.456351",
    "source_id": "IBM_mcp-context-forge",
    "item_type": "pr",
    "item_number": 3269,
    "raw_data_path": "data\\raw\\github\\IBM_mcp-context-forge\\prs\\3269.json"
  }
}